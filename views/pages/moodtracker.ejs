<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Mood Tracker - VibingSG</title>
  <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
  <style>
    body {
      margin: 0;
      font-family: 'Poppins', sans-serif;
      background: #f5f5f5;
    }
    header {
     display: flex;
            justify-content: space-between;
            align-items: center;
            background: linear-gradient(90deg, #7b2ff7, #f107a3);
            color: white;
            padding: 30px 20px;
            border-top-left-radius: 20px;
            border-top-right-radius: 20px;
            box-shadow: 0 2px 5px rgba(0,0,0,0.1);
    }
    .logo span {
      color: #66f0ff;
    }
    nav a {
      color: white;
      margin-left: 15px;
      text-decoration: none;
    }
    main {
      padding: 20px;
    }
    .mood-tracker, .log-mood-form, .mood-insights {
      background: white;
      border-radius: 10px;
      padding: 20px;
      box-shadow: 0 2px 5px rgba(0,0,0,0.1);
      margin-bottom: 20px;
    }
    .section-title {
      color: #7b2ff7;
      font-weight: 600;
      margin-bottom: 10px;
    }
    .moods {
      display: flex;
      justify-content: space-around;
      margin: 15px 0;
    }
    .mood {
      padding: 10px;
      border-radius: 50%;
      background: #f0f0f0;
      cursor: pointer;
      text-align: center;
    }
    .mood.active {
      background: #66f0ff;
      color: white;
    }
    .intensity {
      display: flex;
      justify-content: center;
      margin: 15px 0;
    }
    .intensity button {
      background: none;
      border: 2px solid #ccc;
      border-radius: 50%;
      width: 40px;
      height: 40px;
      margin: 0 5px;
      font-weight: bold;
      cursor: pointer;
    }
    .intensity button.active {
      background: #f107a3;
      color: white;
      border: none;
    }
    textarea {
      width: 100%;
      height: 80px;
      margin-top: 10px;
      border-radius: 5px;
      border: 1px solid #ccc;
      padding: 10px;
      font-family: 'Poppins', sans-serif;
    }
    button.save-mood {
      margin-top: 20px;
      background: linear-gradient(90deg, #7b2ff7, #f107a3);
      color: white;
      border: none;
      padding: 10px 20px;
      border-radius: 10px;
      cursor: pointer;
    }
  </style>
</head>
<body>
  <header>
    <div class="logo" style="font-size: 1.8em;">Vibing<span>SG</span></div>
    <nav>
      <a href="index.html">Home</a>
      <a href="toolkits.html">Resources</a>
      <a href="support.html">Helpline</a>
    </nav>
  </header>

  <main>
    <div class="mood-tracker">
      <h2 class="section-title">Mood Tracker</h2>
      <canvas id="moodChart" height="100"></canvas>
    </div>

    <div class="log-mood-form">
      <h2 class="section-title">Log Your Mood</h2>
      <p>How are you vibing today?</p>
      <div class="moods">
        <div class="mood" data-mood="Happy">Happy</div>
        <div class="mood" data-mood="Calm">Calm</div>
        <div class="mood" data-mood="Neutral">Neutral</div>
        <div class="mood" data-mood="Anxious">Anxious</div>
        <div class="mood" data-mood="Stressed">Stressed</div>
      </div>

      <p>How intense is this feeling? (1-5)</p>
      <div class="intensity">
        <button>1</button>
        <button>2</button>
        <button class="active">3</button>
        <button>4</button>
        <button>5</button>
      </div>

      <p>What triggered this feeling?</p>
      <textarea placeholder="Describe what's happening in your day..."></textarea>

      <div style="text-align: center;">
        <button class="save-mood">Save Mood Entry</button>
      </div>
    </div>

    <div class="mood-insights">
      <h2 class="section-title">Mood Insights</h2>
      <p><strong>Recommendations</strong></p>
 
      <ul>
        <li>🧘‍♂️ Try deep breathing exercises</li>
        <li>📓 Evening journaling before sleep</li>
        <li>⏲️ Pomodoro technique for studying</li>
      </ul>
    </div>
  </main>
  
<footer style="background: linear-gradient(90deg, #7b2ff7, #f107a3); color: white; padding: 30px 20px; border-bottom-left-radius: 20px; border-bottom-right-radius: 20px; font-size: 1em; text-align: center;">
  <p style="max-width: 700px; margin: auto;">
    A mental wellness platform designed for Singaporean teenagers to manage academic stress through mood tracking, journaling, self-help resources, and educational content.
  </p>
</footer>

  <script>
    const ctx = document.getElementById('moodChart').getContext('2d');
    const moodChart = new Chart(ctx, {
      type: 'line',
      data: {
        labels: ['Mon', 'Tue', 'Wed', 'Thu', 'Fri', 'Sat', 'Sun'],
        datasets: [{
          label: 'Mood Intensity',
          data: [2, 4, 3, 5, 4, 5, 2],
          backgroundColor: 'rgba(123, 47, 247, 0.2)',
          borderColor: '#7b2ff7',
          borderWidth: 2,
          pointBackgroundColor: '#f107a3',
          tension: 0.4
        }]
      },
      options: {
        scales: {
          y: { beginAtZero: true, suggestedMax: 5 }
        }
      }
    });

    const moods = document.querySelectorAll('.mood');
    moods.forEach(mood => {
      mood.addEventListener('click', () => {
        moods.forEach(m => m.classList.remove('active'));
        mood.classList.add('active');
      });
    });

    const intensityBtns = document.querySelectorAll('.intensity button');
    intensityBtns.forEach(btn => {
      btn.addEventListener('click', () => {
        intensityBtns.forEach(b => b.classList.remove('active'));
        btn.classList.add('active');
      });
    });
  </script>
</body>
</html>
